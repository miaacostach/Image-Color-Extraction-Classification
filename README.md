# Image-Color-Extraction-Classification
A lightweight Python project that analyzes images to automatically detect their dominant colors and classify them based on their color palettes. It combines computer vision with clustering techniques to transform raw images into meaningful color data.
